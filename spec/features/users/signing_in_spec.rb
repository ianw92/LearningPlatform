require "rails_helper"

feature 'Signing in' do
  feature "Signing in with valid credentials" do
    scenario "redirects you to the modules index page" do

    end

    scenario "shows a message telling you that you have successfully signed in" do

    end
  end

  feature "Signing in with invalid credentials" do
    scenario "rerenders the sign in page" do

    end

    scenario "shows a message telling you that your details are incorrect" do

    end
  end
end
