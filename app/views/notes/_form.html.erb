<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :note_user_id %>
  </div>

  <div class="field">
    <%= form.label :lecture_module_id %>
    <%= form.text_field :lecture_module_id, id: :note_lecture_module_id %>
  </div>

  <div class="field">
    <%= form.label :week %>
    <%= form.number_field :week, id: :note_week %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes, id: :note_notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
