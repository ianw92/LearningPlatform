<%= form_with(model: note, local: true) do |form| %>
  <% if note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.hidden_field :user_id, id: :note_user_id, value: user.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :lecture_module_id, id: :note_lecture_module_id, value: lecture_module.id %>
  </div>

  <div class="field">
    <%= form.hidden_field :week, id: :note_week, value: week %>
  </div>

  <div class="field">
    <%= form.text_area :notes, id: :notes_textarea, value: note.notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
