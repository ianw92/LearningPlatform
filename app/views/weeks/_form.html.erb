<%= form_with(model: week, local: true) do |form| %>
  <% if week.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(week.errors.count, "error") %> prohibited this week from being saved:</h2>

      <ul>
      <% week.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :lecture_module_id %>
    <%= form.text_field :lecture_module_id, id: :week_lecture_module_id %>
  </div>

  <div class="field">
    <%= form.label :week_number %>
    <%= form.number_field :week_number, id: :week_week_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
